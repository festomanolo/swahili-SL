### table08b_per_class_metrics_multiseed

Per-class metrics as mean +/- s.d. over 1 seeds. Preferable to the single-run table above wherever the manuscript can accommodate it, because a single run on 18 clips per class is not a stable estimate.

