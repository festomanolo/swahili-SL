### table_speech_synthesis

Measured behaviour of the pretrained speech stage per class: token count, audio duration, synthesis latency and real-time factor on CPU. These are properties of a released model used unchanged, not results of this study (Reviewer B comments 8 and 9).

