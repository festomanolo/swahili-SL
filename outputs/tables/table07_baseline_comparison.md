### table07_baseline_comparison

MANUSCRIPT TABLE 7 / REVIEWER A COMMENT 7. Every architecture the reviewer named -- MLP, CNN, CNN-LSTM, unidirectional LSTM and GRU, BiGRU, BiLSTM, Transformer encoder, and a skeleton graph network -- trained under one protocol on one corpus with one split. This is the only table in the paper that supports a comparative claim; the cross-study positioning table does not.

