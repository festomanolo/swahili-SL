### table_split_per_class

Original (un-augmented) clips per class in each partition. The test column is the support column of the per-class metrics table and of the confusion matrix, so the three are guaranteed to agree.

