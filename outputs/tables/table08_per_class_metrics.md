### table08_per_class_metrics

Manuscript Table 8. Per-class precision, recall and F1 on the held-out test partition, with the absolute correct/error counts alongside, so the percentages can be read against the 18-clip support they rest on (Reviewer B comment 13).

