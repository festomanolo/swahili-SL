### table_augmentation_variants

Manuscript Figure 3(a) as a table: what each augmentation variant does and how many training sequences it contributes. Every variant is applied to every training clip, so the contributions are uniform by construction.

