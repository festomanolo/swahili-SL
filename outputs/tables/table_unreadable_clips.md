### table_unreadable_clips

Video files that report zero frames or zero resolution and therefore cannot contribute any landmarks. They are excluded here rather than silently producing all-zero sequences. Check whether these recordings still exist in usable form; if not, the corpus count in Section 3.2.3 must be reduced accordingly.

