### table_bootstrap_confidence_intervals

Percentile bootstrap (2,000 resamples of the 126 test clips) on each model accuracy, and on the paired difference from the proposed model. The difference interval is the decisive column: where it contains zero, the manuscript must not claim an advantage.

