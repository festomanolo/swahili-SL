### table_label_to_speech_mapping

The fixed lookup table that converts a class index into Swahili text. No language model and no text generation are involved, so the mapping is deterministic and lossless -- which is what Section 3.7 asserts and this table verifies.

